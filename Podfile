# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'StocksPrice' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StocksPrice
    pod 'Reusable'
    pod 'SwiftLint'
    pod 'MBProgressHUD', '~> 1.2.0'
    pod 'Alamofire', '~> 5.2'
    pod 'OHHTTPStubs/Swift' # includes the Default subspec, with support for NSURLSession & JSON, and the Swiftier API wrappers
  target 'StocksPriceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StocksPriceUITests' do
    # Pods for testing
  end

end
